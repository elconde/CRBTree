#include "crbtree.h"

node::node () {
    parent = 0;
}

// node::grandparent( node& result) {
//     if ( n->parent != 0 ) {
//         result = n->parent->parent; 
//     }
// }
int main() {
    return 0;
}
